# The Weekly Docket — week of 2026-08-31

## Status: No data — GovInfo API unreachable

This week's Docket could not be produced. The GovInfo API (`https://api.govinfo.gov`) refused every request made during this run with `HTTP 429 OVER_RATE_LIMIT`, using the shared `DEMO_KEY`. No `GOVINFO_API_KEY` environment variable was configured as a fallback credential.

Two requests to the `PLAW` collection endpoint were attempted, at approximately 13:07 and 13:08 UTC, spaced 20 seconds apart (one initial attempt plus the one permitted retry). Both attempts returned the same response body:

```json
{
  "error": {
    "code": "OVER_RATE_LIMIT",
    "message": "You have exceeded your rate limit. Try again later or contact us at govinfo@gpo.gov for assistance"
  }
}
```

No bill or public-law data was ever successfully retrieved. This is the same failure mode logged in last week's edition (2026-08-24), suggesting the shared `DEMO_KEY` is exhausted globally rather than by this session's own request volume. In keeping with this column's rule against fabricated or reconstructed-from-memory analysis, no GOOD / BAD / UGLY review is presented this week. No claims are made about what Congress did or didn't do in the period since 2026-08-24.

**Recommended fix for next run:** supply a registered `GOVINFO_API_KEY` (via api.data.gov) instead of relying on the shared `DEMO_KEY`. This is the second consecutive week this has blocked publication.

## By the numbers

| Metric | Value |
|---|---|
| SINCE (window start) | 2026-08-24T00:00:00Z |
| Report date | 2026-08-31 |
| API requests attempted | 2 |
| API requests succeeded | 0 |
| Public laws retrieved | 0 |
| Bills retrieved | 0 |
| Items analyzed | 0 |
| Items skipped under cap | 0 |

## Sources

- `GET https://api.govinfo.gov/collections/PLAW/2026-08-24T00:00:00Z?pageSize=100&offsetMark=*&api_key=DEMO_KEY` — attempted 2 times (initial + 1 retry, ~20s apart), both returned `HTTP 429 OVER_RATE_LIMIT`. No other endpoints were reached, since this one never succeeded.
