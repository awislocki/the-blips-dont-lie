# The Weekly Docket — week of 2026-08-24

## Status: No data — GovInfo API unreachable

This week's Docket could not be produced. The GovInfo API (`https://api.govinfo.gov`) refused every request made during this run with `HTTP 429 OVER_RATE_LIMIT`, using the shared `DEMO_KEY`. No `GOVINFO_API_KEY` environment variable was configured as a fallback credential.

Nine requests to the `PLAW` collection endpoint were attempted between roughly 00:00 and 00:08 of the run, spaced with increasing backoff (10s, 10s, 10s, 10s, 30s, 60s, 90s, 180s between attempts). Every attempt returned the same response body:

```json
{
  "error": {
    "code": "OVER_RATE_LIMIT",
    "message": "You have exceeded your rate limit. Try again later or contact us at govinfo@gpo.gov for assistance"
  }
}
```

No bill or public-law data was ever successfully retrieved. In keeping with this column's rule against fabricated or reconstructed-from-memory analysis, no GOOD / BAD / UGLY review is presented this week. No claims are made about what Congress did or didn't do in the period since 2026-08-17.

**Recommended fix for next run:** supply a registered `GOVINFO_API_KEY` (via api.data.gov) instead of relying on the shared `DEMO_KEY`, which appears to be exhausted globally rather than by this session's own request volume.

## By the numbers

| Metric | Value |
|---|---|
| SINCE (window start) | 2026-08-17T00:00:00Z |
| Report date | 2026-08-24 |
| API requests attempted | 9 |
| API requests succeeded | 0 |
| Public laws retrieved | 0 |
| Bills retrieved | 0 |

## Sources

- `GET https://api.govinfo.gov/collections/PLAW/2026-08-17T00:00:00Z?pageSize=100&offsetMark=*&api_key=DEMO_KEY` — attempted 9 times, all returned `HTTP 429 OVER_RATE_LIMIT`. No other endpoints were reached, since this one never succeeded.
